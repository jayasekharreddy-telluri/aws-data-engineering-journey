
-- create
CREATE TABLE employees (
    id INT,
    name VARCHAR(50),
    department VARCHAR(50),
    salary INT
);

INSERT INTO employees VALUES
(1, 'A', 'IT', 5000),
(2, 'B', 'IT', 6000),
(3, 'C', 'IT', 4000),
(4, 'D', 'HR', 3000),
(5, 'E', 'HR', 3500),
(6, 'F', 'HR', 3000),
(7, 'G', 'Sales', 4500),
(8, 'H', 'Sales', 7000);


select* from employees;

select name,department,salary,avg(salary) over(partition by department) from employees;

select name,department,salary,row_number()  over(partition by department order by salary desc) as_row_numeber from employees;

select name,department,salary, dense_rank() over(partition by department order by salary desc) as dense_rank from employees;

select * from  (select name,department,salary,dense_rank() over(partition by department order by salary desc) as dense_rank from employees) where dense_rank <=2;

select name,department,salary,lag(salary) over(partition by department order by salary asc) as prev_salary from employees;

select name,department,salary,lag(salary) over(partition by department order by salary desc) as prev_salary ,

salary - lag(salary) over(partition by department order by salary desc) as diff

from employees;



practise -- 1


1. Find all employees whose salary is greater than 50000.

2. Find all employees who belong to the IT department.

3. Find employees whose salary is greater than the average salary of all employees.

4. Find employees whose salary is greater than the average salary of their own department.

5. Find the second highest salary using a subquery.

6. Find employees whose salary is greater than department average using a window function.

7. Find the second highest salary using a window function.

8. Find the top 3 highest-paid employees in each department.

9. Find the cumulative salary (running total).

10. Find the difference between current salary and previous salary.

11. Find employees who have the highest salary in their department.

12. Divide employees into 4 salary groups.






