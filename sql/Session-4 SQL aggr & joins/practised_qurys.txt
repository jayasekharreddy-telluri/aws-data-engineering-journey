-- Online SQL Editor to Run SQL Online.
-- Use the editor to create new tables, insert data and all other SQL operations.


--select * from customers where country = 'USA';

--select * from customers where age > 25;

--select * from customers where country = 'USA' and age > 25;

--select * from customers order by age desc;

--select distinct country from customers;

--select * from customers where age between 22 and 28;

--select * from customers where country = 'USA' or  age < 25;

--select * from customers where country != 'USA';

--select country,count(customer_id) as count  from customers group by country;


--select country,count(customer_id) as count  from customers group by country order by count desc;


--select country,count(customer_id) as count  from customers group by country having count > 1;

--select c.first_name, o.item from customers as c inner join orders as o on c.customer_id = o.customer_id;

--select c.first_name, o.item from customers as c inner join orders as o on c.customer_id = o.customer_id where c.country = 'USA';


--select c.first_name, o.item from customers as c left join orders as o on c.customer_id = o.customer_id;

--select c.first_name, o.item from customers as c right join orders as o on c.customer_id = o.customer_id;

--select c.first_name, o.item, s.status  from customers as c inner join orders as o on c.customer_id = o.customer_id inner join shippings as s on s.customer = o.customer_id;

--select c.customer_id,c.first_name,sum(o.amount) from customers as c inner join orders as o on c.customer_id = o.customer_id group by c.customer_id,c.first_name;

--select c.customer_id,c.first_name,sum(o.amount) from customers as c inner join orders as o on c.customer_id = o.customer_id group by c.customer_id,c.first_name having sum(o.amount) > 500;


--select c.customer_id,c.first_name,count(o.customer_id) from customers as c left join orders as o on c.customer_id = o.customer_id group by c.customer_id,c.first_name having count(o.customer_id) = 0;


--select c.customer_id,c.first_name,count(o.customer_id) from customers as c inner join orders as o on c.customer_id = o.customer_id group by c.customer_id,c.first_name having count(o.customer_id) >= 1;


--select distinct c.customer_id,c.first_name from customers as c inner join orders as o on c.customer_id = o.customer_id;

--select * from customers where first_name like 'J%';


--select * from customers order by age limit 2;


--select * from customers where age > (select avg(age) as avg_age  from customers);

--select * from customers where age = (select distinct age from customers order by age desc limit 1 offset 1);

--select customer_id from customers union all select customer_id from orders;

--select c.last_name from customers as c  join customers as c1 on c1.last_name = c.last_name and c.customer_id != c1.customer_id;

--select * from customers where age =(select distinct age from customers order by age desc limit 1 offset 1);





 









 









